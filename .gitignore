# Dependencies and large data (if using node/npm for testing)
/node_modules

# Operating System Files (Mac/Windows)
.DS_Store
Thumbs.db

# IDE and Temporary Files
.vscode/

# Local storage for your PWA (if generated by the app)
# The PWA uses localStorage in the browser, but if you saved a local file, ignore it
*.log
*.swp

# API Keys - CRITICAL for security!
# If you were to store your Gemini Key directly in a file, IGNORE IT.
# Since your script.js uses an input field, this may not be needed, but it's a good practice.
# .env
# .env.local